services:
  db:
    image: postgres:16
    container_name: mini_crm_db
    environment:
      POSTGRES_USER: crm_user
      POSTGRES_PASSWORD: crm_pass
      POSTGRES_DB: mini_crm
    ports:
      - "5432:5432"
    volumes:
      - mini_crm_data:/var/lib/postgresql/data

volumes:
  mini_crm_data:
